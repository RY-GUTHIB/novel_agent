# novel_agent/requirements.txt
# 核心依赖
python-dotenv>=1.0.0
openai>=1.0.0
tiktoken>=0.7.0
numpy>=1.24.0

# RAG（如需向量检索，需确认 vendor/models/ 下模型文件存在）
chromadb>=0.5.0
rank_bm25>=0.2.0

# 嵌入模型推理（ONNX 后端，轻量无 torch）
onnxruntime>=1.18.0
tokenizers>=0.21.0

# 可选 LLM 后端（按需取消注释）
# google-generativeai>=0.8.0  # Gemini
# anthropic>=0.40.0           # Claude

# GUI（可选，需要 flet 运行时，约 50MB）
# flet>=0.85.0
